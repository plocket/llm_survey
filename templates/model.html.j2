<!DOCTYPE html>
<html>
    <head>
        <title>{{model}}</title>
        <link rel="stylesheet" type="text/css" href="style.css">
    </head>
    <body>
        <nav>
            <ul>
                {% for model in models %}
                <li><a href="{{ model }}.html">{{ model }}</a></li>
                {% endfor %}
            </ul>
        </nav>
        <main>
            <p>Model: {{ model }}</p>

            <p>Prompt</p>
            <div class="markdown-body">{{ prompt|safe }}</div>
            {% for response in responses %}
                <p>Response {{ loop.index }}.</p>
                <div class="markdown-body">{{ response|safe }}</div>
            {% endfor %}
        </main>
    </body>
</html>
